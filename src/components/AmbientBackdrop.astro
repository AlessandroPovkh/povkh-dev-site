<div class="aurora-backdrop" data-aurora-backdrop aria-hidden="true">
  <span class="aurora-field aurora-field-rose" data-aurora-field></span>
  <span class="aurora-field aurora-field-violet" data-aurora-field></span>
</div>

<style is:global>
  html:has(.aurora-backdrop) { background: #10090f; }
  body:has(.aurora-backdrop) {
    isolation: isolate;
    background-color: transparent;
    background-image: none;
  }
  .aurora-backdrop {
    position: fixed;
    z-index: -1;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
    background: linear-gradient(145deg, #140b13 0%, #0b090d 52%, #130a12 100%);
    contain: strict;
  }
  .aurora-field {
    position: absolute;
    display: block;
    width: max(110vw, 88rem);
    height: max(82vh, 52rem);
    border-radius: 50%;
    opacity: 1;
    pointer-events: none;
    will-change: transform, opacity;
  }
  .aurora-field-rose {
    top: -45%;
    right: -54%;
    background: radial-gradient(ellipse at center, rgb(202 27 113 / 0.28) 0%, rgb(109 25 72 / 0.18) 38%, transparent 70%);
    animation: auroraRoseDrift 18s ease-in-out infinite alternate;
  }
  .aurora-field-violet {
    bottom: -48%;
    left: -52%;
    background: radial-gradient(ellipse at center, rgb(111 59 167 / 0.22) 0%, rgb(44 30 91 / 0.14) 42%, transparent 72%);
    animation: auroraVioletDrift 24s ease-in-out -7s infinite alternate;
  }
  @keyframes auroraRoseDrift {
    from { transform: translate3d(0, 0, 0) scale(1); }
    to { transform: translate3d(-6%, 5%, 0) scale(1.08); }
  }
  @keyframes auroraVioletDrift {
    from { transform: translate3d(0, 0, 0) scale(1); }
    to { transform: translate3d(5%, -4%, 0) scale(1.1); }
  }
  @media (max-width: 40rem) {
    .aurora-field { width: 150vw; min-width: 0; height: 86vh; }
    .aurora-field-rose {
      right: -72%;
      background: radial-gradient(ellipse at center, rgb(202 27 113 / 0.224) 0%, rgb(109 25 72 / 0.144) 38%, transparent 70%);
    }
    .aurora-field-violet {
      left: -68%;
      background: radial-gradient(ellipse at center, rgb(111 59 167 / 0.176) 0%, rgb(44 30 91 / 0.112) 42%, transparent 72%);
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .aurora-field { animation: none; }
  }
</style>
